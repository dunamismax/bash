#!/usr/bin/env bash
#
# ~/.xprofile
# Sourced by LightDM. Ensures autorandr is run on login.

# Run autorandr on session start
if command -v autorandr >/dev/null 2>&1; then
    autorandr --change
fi

# Optionally, if you want a fallback for the rare case that autorandr doesn't detect:
# xrdb -merge ~/.Xresources-1080p
